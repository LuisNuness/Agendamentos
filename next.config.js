// next.config.js
const nextConfig = {
    output: 'export', // Gera arquivos estáticos para exportação
    trailingSlash: true, // Adiciona uma barra no final das URLs (opcional)
    // Outras configurações do Next.js (se necessário)

    output: 'export',
    assetPrefix: './', // Usa caminhos relativos
  }
  module.exports = nextConfig